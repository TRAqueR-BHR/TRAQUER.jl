# src/TRAQUERUtil/Conf/s3-path
When TRAQUER is configured to work with S3 storage (as opposed to filesystem directories,
see `src/TRAQUERUtil/Conf/fs-path`), this folder contains helper functions to retrieve the
S3 paths used by TRAQUER.

# src/TRAQUERUtil/Conf/fs-path
When TRAQUER is configured to work with filesystem directories (as opposed to S3 storage,
see `src/TRAQUERUtil/Conf/s3-path`), this folder contains helper functions to retrieve the
filesystem paths used by TRAQUER.
